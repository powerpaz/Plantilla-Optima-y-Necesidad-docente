// Copia este archivo como config.js y ajusta lo necesario.
// Por defecto, si NO existe window.env (o está vacío), se usará el CSV local en /data/.
window.env = {
  // Si quieres usar Supabase, descomenta y configura:
  // SUPABASE_URL: "https://xxxx.supabase.co",
  // SUPABASE_KEY: "eyJhbGciOi...",
  // TABLE: "instituciones"
};
